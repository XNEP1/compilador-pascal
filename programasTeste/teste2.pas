program varsGlobais (input, output);
var a, b: integer;
    k1, temp : boolean;
    procedure procedurefalse(a,b,c:integer);
    var outra,coisa:integer;
    begin
        outra := 10;
        coisa := outra + a;
    end;
begin
    a:=10 + 5 * (10 + 5);
    a:= 10;
    b:=10 +++++++++ 20;
    
    procedurefalse(a+a, a ,a);

end.
